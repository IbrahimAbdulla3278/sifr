{
  "buildCommand": "cd frontend && npm install && npx ng build --configuration production",
  "outputDirectory": "frontend/dist/ecommerce-frontend/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "framework": null
}
